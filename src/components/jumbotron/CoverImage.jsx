import styled from "styled-components";
import Row from "../../grid/Row";

const CoverImage = styled(Row)`
  background: #3e495e;
  border-radius: 25px;
  grid-row: 1 / 3;
`;

export default CoverImage;
